# Holds jobs that run from the scheduled pipeline

build_idf_v5.3_mqtt_tcp_scheduled:
  variables:
    EXAMPLE_CI_FILE: "sdkconfig.ci.p4_wifi"
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_VER: ["v5.3", "v5.3.1", "v5.3.2", "v5.3.3", "v5.3.4", "release-v5.3"]
        IDF_TARGET: ["esp32p4", "esp32h2"]
        IDF_SLAVE_TARGET: ["esp32c6"]
        IDF_EXAMPLE_PATH: ["examples/protocols/mqtt/tcp"]

build_idf_v5.4_mqtt_tcp_scheduled:
  variables:
    EXAMPLE_CI_FILE: "sdkconfig.ci.p4_wifi"
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_VER: ["v5.4", "v5.4.1", "v5.4.2", "release-v5.4"]
        IDF_TARGET: ["esp32p4", "esp32h2"]
        IDF_SLAVE_TARGET: ["esp32c6"]
        IDF_EXAMPLE_PATH: ["examples/protocols/mqtt/tcp"]

build_idf_v5.5_iperf_scheduled:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_VER: ["v5.5", "release-v5.5"]
        IDF_TARGET: ["esp32p4", "esp32h2"]
        IDF_SLAVE_TARGET: ["esp32", "esp32c2", "esp32c3", "esp32s3" ]
        IDF_EXAMPLE_PATH: ["examples/wifi/iperf"]

build_idf_master_mqtt_tcp_scheduled:
  variables:
    EXAMPLE_CI_FILE: "sdkconfig.ci.p4_wifi"
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template
  image: espressif/idf:latest
  parallel:
    matrix:
      - IDF_TARGET: ["esp32p4", "esp32h2"]
        IDF_SLAVE_TARGET: ["esp32", "esp32c2", "esp32c3", "esp32s3" ]
        IDF_EXAMPLE_PATH: ["examples/protocols/mqtt/tcp"]

build_idf_master_iperf_scheduled:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template
  image: espressif/idf:latest
  parallel:
    matrix:
      - IDF_TARGET: ["esp32p4", "esp32h2"]
        IDF_SLAVE_TARGET: ["esp32", "esp32c2", "esp32c3", "esp32s3" ]
        IDF_EXAMPLE_PATH: ["examples/wifi/iperf"]

build_coprocessor_idf_v5.3_scheduled:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template_coprocessor
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_TARGET: ["esp32c6"]
        IDF_VER: ["v5.3", "v5.3.1", "v5.3.2", "v5.3.3", "v5.3.4", "release-v5.3"]
        SLAVE_CI_FILE: ["sdio", "spi", "spi_hd", "uart", "dpp"]

build_coprocessor_idf_v5.3_scheduled_2:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template_coprocessor
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_TARGET: ["esp32c2", "esp32c3", "esp32s3"]
        IDF_VER: ["v5.3", "v5.3.1", "v5.3.2", "v5.3.3", "v5.3.4", "release-v5.3"]
        SLAVE_CI_FILE: ["spi", "spi_hd", "uart"]

build_coprocessor_idf_v5.3_esp32_scheduled:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template_coprocessor
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_TARGET: ["esp32"]
        IDF_VER: ["v5.3", "v5.3.1", "v5.3.2", "v5.3.3", "v5.3.4", "release-v5.3"]
        SLAVE_CI_FILE: ["sdio", "spi", "uart"]

build_coprocessor_idf_v5.4_scheduled:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template_coprocessor
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_TARGET: ["esp32c6"]
        IDF_VER: ["v5.4", "v5.4.1", "v5.4.2", "release-v5.4"]
        SLAVE_CI_FILE: ["sdio", "spi", "spi_hd", "uart", "dpp"]

build_coprocessor_idf_v5.4_scheduled_2:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template_coprocessor
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_TARGET: ["esp32c2", "esp32c3", "esp32s3"]
        IDF_VER: ["v5.4", "v5.4.1", "v5.4.2", "release-v5.4"]
        SLAVE_CI_FILE: ["spi", "spi_hd", "uart"]

build_coprocessor_idf_v5.4_esp32_scheduled:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template_coprocessor
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_TARGET: ["esp32"]
        IDF_VER: ["v5.4", "v5.4.1", "v5.4.2", "release-v5.4"]
        SLAVE_CI_FILE: ["sdio", "spi", "uart"]

build_coprocessor_idf_v5.5_scheduled:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template_coprocessor
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_TARGET: ["esp32c2", "esp32c3", "esp32s3"]
        IDF_VER: ["v5.5", "v5.5.1", "release-v5.5"]
        SLAVE_CI_FILE: ["spi", "spi_hd", "uart"]

build_coprocessor_idf_v5.5_esp32_scheduled:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template_coprocessor
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_TARGET: ["esp32"]
        IDF_VER: ["v5.5", "v5.5.1", "release-v5.5"]
        SLAVE_CI_FILE: ["sdio", "spi", "uart"]

build_coprocessor_idf_master_all_features_enabled_scheduled:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template_coprocessor
  image: espressif/idf:latest
  parallel:
    matrix:
      - IDF_TARGET: ["esp32c6"]
        SLAVE_CI_FILE: ["all_features"]

build_nimble_examples:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template_example
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_TARGET: ["esp32p4", "esp32h2"]
        IDF_VER: ["v5.4", "v5.4.1", "v5.4.2", "release-v5.4",
        "v5.3", "v5.3.1", "v5.3.2", "v5.3.3", "v5.3.4", "release-v5.3"]
        IDF_SLAVE_TARGET: ["esp32c6"]
        EXAMPLE_TO_BUILD: ["host_nimble_bleprph_host_only_vhci",
        "host_nimble_bleprph_host_only_uart_hci"]

build_bluedroid_examples:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template_example
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_TARGET: ["esp32p4", "esp32h2"]
        IDF_VER: ["v5.4", "v5.4.1", "v5.4.2", "release-v5.4",
        "v5.3", "v5.3.1", "v5.3.2", "v5.3.3", "v5.3.4", "release-v5.3"]
        IDF_SLAVE_TARGET: ["esp32"]
        EXAMPLE_TO_BUILD: ["host_bluedroid_ble_compatibility_test",
        "host_bluedroid_bt_hid_mouse_device",
        "host_bluedroid_host_only",
        "host_bt_controller_mac_addr"]

build_wifi_examples:
  tags:
    - build
  rules:
    - !reference [.staging_branch_rules, rules]
  extends: .build_template_example
  image: espressif/idf:${IDF_VER}
  parallel:
    matrix:
      - IDF_TARGET: ["esp32p4", "esp32h2"]
        IDF_VER: ["v5.4", "v5.4.1", "v5.4.2", "release-v5.4",
        "v5.3", "v5.3.1", "v5.3.2", "v5.3.3", "v5.3.4", "release-v5.3"]
        IDF_SLAVE_TARGET: ["esp32c6", "esp32c5"]
        EXAMPLE_TO_BUILD: ["host_wifi_easy_connect_dpp_enrollee",
        "host_wifi_itwt",
        "host_transport_config",
        "host_network_split__power_save"]

# ESP-IDF build artifacts
build/
managed_components/
dependencies.lock

# ESP-IDF config
sdkconfig
sdkconfig.old

# IDE files
.vscode/
.idea/

# SquareLine Studio project files (only export the UI, not the project)
*.spj
